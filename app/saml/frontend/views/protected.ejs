<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Protected Resource</title>
  <link href="https://fonts.googleapis.com/css?family=Roboto:700,400&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body { font-family: 'Roboto', sans-serif; background: linear-gradient(135deg, #43cea2 0%, #185a9d 100%); margin: 0; padding: 0; }
    .container { max-width: 420px; margin: 60px auto; background: #fff; border-radius: 16px; box-shadow: 0 4px 24px rgba(0,0,0,0.12); padding: 36px 32px; text-align: center; }
    h2 { color: #1976d2; margin-bottom: 18px; font-size: 2em; }
    .user-info { background: #f4f6fb; border-radius: 10px; padding: 18px; margin: 18px 0; box-shadow: 0 2px 8px rgba(67,206,162,0.08); }
    .user-info i { color: #43cea2; font-size: 1.5em; margin-bottom: 8px; }
    .username { font-size: 1.2em; color: #185a9d; font-weight: 700; }
    .meta { margin-top: 28px; font-size: 1em; color: #888; }    .btn { display: inline-block; background: #43cea2; color: #fff; border: none; padding: 10px 28px; border-radius: 8px; font-size: 1em; cursor: pointer; margin: 8px; text-decoration: none; font-weight: 700; transition: background 0.2s; }
    .btn:hover { background: #1976d2; }
    .btn-logout { background: #f44336; }
    .btn-logout:hover { background: #d32f2f; }
  </style>
</head>
<body>
  <div class="container">
    <h2><i class="fa-solid fa-lock"></i> Protected Resource</h2>
    <div class="user-info">
      <i class="fa-solid fa-user"></i>
      <div class="username"><%= user.username %></div>
      <div>Welcome, you have successfully signed in via SAML SSO!</div>    </div>
    <a href="/" class="btn"><i class="fa-solid fa-arrow-left"></i> Back to Home</a>
    <a href="/logout" class="btn btn-logout"><i class="fa-solid fa-sign-out-alt"></i> Logout</a>
    <div class="meta">SAML SP Demo | Powered by <b>samlify</b> &amp; Express</div>
  </div>
</body>
</html>
